<section class="hero" id="hero">
  <div class="hero-glow" />
  <div class="hero-content">
    <div class="hero-kicker">⚖ OASIS OMNIVERSE · JUSTICE LEAGUE ACADEMY</div>
    <h1 class="hero-title">Train Your Mind.<br><em>Master Justice.</em></h1>
    <p class="hero-sub">Six disciplines. Real skills. OASIS karma rewards. Justice League Academy is the world's first blockchain-verified martial arts, law, and leadership training platform.</p>
    <div class="hero-ctas">
      <a href="#courses" class="btn-primary">⚖ Start Training</a>
      <a href="#disciplines" class="btn-outline">Explore Disciplines</a>
    </div>
    <div class="hero-stats">
      <div class="hs"><span class="hs-n">24,800</span><span class="hs-l">Active Students</span></div>
      <div class="hs-div" />
      <div class="hs"><span class="hs-n">6</span><span class="hs-l">Core Disciplines</span></div>
      <div class="hs-div" />
      <div class="hs"><span class="hs-n">142</span><span class="hs-l">Countries</span></div>
    </div>
  </div>
</section>

<style>
  .hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 120px 40px 80px; position: relative; text-align: center; }
  .hero-glow { position: absolute; top: 20%; left: 50%; transform: translateX(-50%); width: 600px; height: 400px; background: radial-gradient(ellipse, rgba(59,130,246,.1) 0%, rgba(168,85,247,.06) 50%, transparent 70%); pointer-events: none; }
  .hero-content { position: relative; z-index: 1; max-width: 780px; }
  .hero-kicker { font-family: 'Share Tech Mono', monospace; font-size: 11px; letter-spacing: .2em; color: #3b82f6; margin-bottom: 24px; }
  .hero-title { font-family: 'Orbitron', sans-serif; font-size: clamp(36px,6vw,80px); font-weight: 900; color: #fff; line-height: 1.1; margin: 0 0 24px; letter-spacing: .04em; }
  .hero-title em { background: linear-gradient(135deg,#3b82f6,#a855f7); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-style: normal; }
  .hero-sub { font-family: 'Rajdhani', sans-serif; font-size: clamp(16px,2vw,20px); color: #a8bfd8; line-height: 1.7; margin: 0 auto 40px; max-width: 620px; }
  .hero-ctas { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; margin-bottom: 56px; }
  .btn-primary { background: linear-gradient(135deg,#3b82f6,#a855f7); color: #fff; padding: 16px 36px; border-radius: 10px; font-family: 'Orbitron',sans-serif; font-size: 13px; font-weight: 700; letter-spacing: .1em; text-decoration: none; }
  .btn-outline { border: 1px solid rgba(59,130,246,.4); color: #3b82f6; padding: 16px 36px; border-radius: 10px; font-family: 'Orbitron',sans-serif; font-size: 13px; font-weight: 700; letter-spacing: .1em; text-decoration: none; transition: background .2s; }
  .btn-outline:hover { background: rgba(59,130,246,.08); }
  .hero-stats { display: flex; align-items: center; justify-content: center; gap: 32px; }
  .hs { text-align: center; }
  .hs-n { font-family: 'Orbitron', sans-serif; font-size: 28px; font-weight: 900; color: #3b82f6; display: block; }
  .hs-l { font-family: 'Share Tech Mono', monospace; font-size: 10px; letter-spacing: .16em; color: #a8bfd8; }
  .hs-div { width: 1px; height: 40px; background: rgba(59,130,246,.2); }
</style>
